{
  "name": "web-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Spec-first SDK that turns a website into an AI-operable companion — declare tools and resources once, expose them as in-page sidebar (with a visible cursor), MCP server, and CLI.",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm -r --parallel run dev",
    "clean": "pnpm -r exec rm -rf dist .turbo *.tsbuildinfo",
    "demo:cross-page": "node scripts/demo-cross-page.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
